# Categories
<% systems.each do |system| %>
<% categories.each do |category| %>
<%= system %>_<%= category.parameterize.underscore %>:
  name: <%= category %>
  tag_category: <%= system %>_categories
  slug: <%= system.parameterize %>-<%= category.parameterize %>
  system: <%= system %>
<% end %>
<% end %>

# Officialness
mork_borg_cult:
  name: Mörk Borg Cult
  tag_category: morkborg_officialness
  slug: mork-borg-cult
  system: morkborg

# Creators
johan_nohr:
  name: Johan Nohr
  tag_category: morkborg_creators
  slug: johan-nohr
  system: morkborg
johnny_carhat:
  name: Johnny Carhat
  tag_category: morkborg_creators
  slug: johnny-carhat
  system: morkborg
<% systems.each do |system| %>
<% 20.times do |index| %>
<% name = Faker::Name.name %>
<%= system %>_tag_creator_<%= index %>:
  name: <%= name %>
  tag_category: <%= system %>_creators
  slug: <%= system.parameterize %>-<%= name.parameterize %>
  system: <%= system %>
<% end %>
<% end %>
