<div class="tw-flex tw-flex-col tw-gap-24">
  <div class="tw-flex tw-flex-col tw-gap-6 align-center">
    <h2 class="tw-font-display tw-text-6xl"><%= t ".categories_heading" %></h2>

    <dl class="switcher | tw-font-body">
      <% @dashboard.categories.in_groups(2, false) do |category_group| %>
        <div class="tw-flex tw-flex-col tw-gap-3">
          <% category_group.each do |category| %>
            <div>
              <dt><%= link_to category.name, category, class: "tw-bg-accent-1", data: { controller: "highlight" } %></dt>
              <dd><%= category.short_description %></dd>
            </div>
          <% end %>
        </div>
      <% end %>
    </dl>
  </div>

  <div class="tw-flex tw-flex-col tw-gap-6 align-center">
    <h2 class="tw-font-display tw-text-6xl"><%= t ".latest_entries_heading" %></h2>
    <p class="tw-font-body"><%= t ".latest_entries_subheading_html" %></p>

    <div class="reel" data-controller="reel">
      <%= render EntryTileComponent.with_collection(@dashboard.recently_created_entries) %>
    </div>
  </div>
</div>
